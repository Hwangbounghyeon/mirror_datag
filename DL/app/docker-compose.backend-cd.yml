services:
  backend:
    image: ${DOCKER_REGISTRY}/data:lts
    env_file:
      - .env
    ports:
      - "8001:8000"
    restart: unless-stopped
    networks:
      - be-redis

networks:
  be-redis:
    external: true
